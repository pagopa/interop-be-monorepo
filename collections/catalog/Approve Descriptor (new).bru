meta {
  name: Approve Descriptor (new)
  type: http
  seq: 93
}

post {
  url: {{host-catalog}}/eservices/:eServiceId/descriptors/:descriptorId/approve
  body: none
  auth: none
}

params:path {
  eServiceId: {{eserviceId}}
  descriptorId: {{descriptorId}}
}

headers {
  Authorization: {{JWT}}
  X-Correlation-Id: {{correlation-id}}
}
