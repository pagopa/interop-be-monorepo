meta {
  name: Suspend Purpose Version
  type: http
  seq: 9
}

post {
  url: {{host-m2m-gw}}/purposes/:purposeId/versions/:versionId/suspend
  body: none
  auth: none
}

params:path {
  purposeId: {{purposeId}}
  versionId: {{versionId}}
}

headers {
  Authorization: {{JWT-M2M}}
  x-correlation-id: {{correlation-id}}
}

docs {
  Suspends the specified purpose version
}
