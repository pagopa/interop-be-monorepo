meta {
  name: Archive Purpose Version
  type: http
  seq: 8
}

post {
  url: {{host-m2m-gw}}/purposes/:purposeId/versions/:versionId/archive
  body: none
  auth: none
}

params:path {
  purposeId: {{purposeId}}
  versionId: {{versionId}}
}

headers {
  Authorization: {{JWT-M2M}}
  x-correlation-id: {{correlation-id}}
}

docs {
  Archives the specified purpose version
}
