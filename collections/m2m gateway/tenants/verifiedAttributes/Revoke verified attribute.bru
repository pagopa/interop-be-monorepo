meta {
  name: Revoke tenant verified attribute
  type: http
  seq: 7
}

delete {
  url: {{host-m2m-gw}}/tenants/:tenantId/verifiedAttributes/:attributeId?agreementId={{agreementId}}
  body: none
  auth: none
}

params:path {
  tenantId: {{tenantId}}
  attributeId: {{attributeId}}
}

params:query {
  agreementId: {{agreementId}}
}

headers {
  Authorization: {{JWT-M2M}}
}
