meta {
  name: Assign verified attribute
  type: http
  seq: 7
}

post {
  url: {{host-m2m-gw}}/tenants/:tenantId/verifiedAttributes
  body: json
  auth: none
}

params:path {
  tenantId: {{tenantId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

body:json {
  {
    "id" : "{{attributeId}}",
    "agreementId" : "{{agreementId}}",
    "expiresAt" : "2030-01-01T00:00:00Z"
  }
}
