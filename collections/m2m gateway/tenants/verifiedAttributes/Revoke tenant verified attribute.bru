meta {
  name: Revoke tenant verified attribute
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/tenants/:tenantId/verifiedAttributes/:attributeId?agreementId
  body: none
  auth: none
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

params:path {
  tenantId: {{tenantId}}
  attributeId: {{attributeId}}
}

params:query {
  agreementId: {{agreementId}}
}

