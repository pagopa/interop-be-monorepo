meta {
  name: Revoke tenant declared attribute
  type: http
  seq: 7
}

delete {
  url: {{host-m2m-gw}}/tenants/:tenantId/declaredAttributes/:attributeId
  body: none
  auth: none
}

params:path {
  tenantId: {{tenantId}}
  attributeId: {{attributeId}}
}

headers {
  Authorization: {{JWT-M2M}}
}
