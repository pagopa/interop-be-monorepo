meta {
  name: Revoke declared attribute
  type: http
  seq: 8
}

delete {
  url: {{host-m2m-gw}}/tenants/declaredAttributes/:attributeId
  body: json
  auth: none
}

params:path {
  attributeId: {{attributeId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

