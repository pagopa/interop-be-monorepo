meta {
  name: Suspend Descriptor
  type: http
  seq: 6
}

post {
  url: {{host-m2m-gw}}/eservices/:eserviceId/descriptors/:descriptorId/suspend
  body: none
  auth: none
}

params:path {
  eserviceId: {{eserviceId}}
  descriptorId: {{descriptorId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
  X-Correlation-Id: {{correlation-id}}
}
