meta {
  name: Archive Purpose Template
  type: http
  seq: 8
}

post {
  url: {{host-m2m-gw}}/purposeTemplates/:purposeTemplateId/archive
  auth: none
}

params:path {
  purposeTemplateId: {{purposeTemplateId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}
