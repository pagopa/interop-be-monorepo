meta {
  name: Get Purpose Template
  type: http
  seq: 4
}

get {
  url: {{host-m2m-gw}}/purposeTemplates/:purposeTemplateId
  body: none
  auth: none
}

params:path {
  purposeTemplateId: {{purposeTemplateId}}
}

headers {
  Authorization: {{JWT-M2M}}
  X-Correlation-Id: {{correlation-id}}
}

vars:post-response {
  purposeTemplateId: res.body.id
}
