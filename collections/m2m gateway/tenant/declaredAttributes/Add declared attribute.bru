meta {
  name: Add declared attribute
  type: http
  seq: 7
}

post {
  url: {{host-m2m-gw}}/tenants/declaredAttributes
  body: json
  auth: none
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

body:json {
  {
    "id" : "{{attributeId}}"
  }
}
