meta {
  name: Unsuspend eservice template version
  type: http
  seq: 11
}

post {
  url: {{host-m2m-gw}}/eserviceTemplates/:templateId/versions/:versionId/unsuspend
  body: json
  auth: none
}

params:path {
  versionId: {{eserviceTemplateVersionId}}
  templateId: {{eserviceTemplateId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

vars:post-response {
  eserviceTemplateVersionId: res.body.id
}
