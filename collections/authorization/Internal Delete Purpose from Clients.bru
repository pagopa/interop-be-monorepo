meta {
  name: Internal Delete Purpose from Clients
  type: http
  seq: 36
}

delete {
  url: {{host-authorization}}/internal/clients/purposes/:purposeId
  body: none
  auth: none
}

params:path {
  purposeId: {{purposeId}}
}

headers {
  Authorization: {{JWT}}
  X-Correlation-Id: {{correlation-id}}
}
