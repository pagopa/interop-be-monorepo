meta {
  name: Create new delegation
  type: http
  seq: 1
}

post {
  url: {{host-bff}}/consumer/delegations
  body: json
  auth: none
}

body:json {
  {
    "eserviceId": "{{eserviceId}}",
    "delegateId": "{{tenantId2}}"
  }
}

headers {
  Authorization: {{JWT}}
  X-Correlation-Id: {{correlation-id}}
}
