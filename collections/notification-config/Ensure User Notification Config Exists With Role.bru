meta {
  name: Ensure User Notification Config Exists With Role
  type: http
  seq: 2
}

post {
  url: {{host-notification-config}}/internal/ensureUserNotificationConfigExistsWithRole
  body: json
  auth: none
}

headers {
  x-correlation-id: {{correlation-id}}
  Authorization: {{JWT}}
}

body:json {
  {
    "userId": "{{userId}}",
    "tenantId": "{{tenantId}}",
    "userRole": "{{userRole}}"
  }
}
