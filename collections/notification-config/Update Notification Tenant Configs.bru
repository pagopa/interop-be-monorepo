meta {
  name: Update Notification Tenant Configs
  type: http
  seq: 2
}

post {
  url: {{host-notification-config}}/notificationTenantConfigs
  body: json
  auth: none
}

headers {
  x-correlation-id: {{correlation-id}}
  Authorization: {{JWT}}
}

body:json {
  {
    "consumer": {
      "eService": {
        "newEServiceVersionPublished": true
      }
    }
  }
}
