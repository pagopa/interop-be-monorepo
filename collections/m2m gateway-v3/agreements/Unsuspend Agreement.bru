meta {
  name: Unsuspend agreement
  type: http
  seq: 9
}

post {
  url: {{host-m2m-gw-v3}}/agreements/:agreementId/unsuspend
  body: json
  auth: none
}

params:path {
  agreementId: {{agreementId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

body:json {
  {
    "delegationId": "{{delegationId}}"
  }
}
