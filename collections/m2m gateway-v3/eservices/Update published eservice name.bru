meta {
  name: Update published eservice name
  type: http
  seq: 1
}

patch {
  url: {{host-m2m-gw-v3}}/eservices/:eserviceId/name
  body: json
  auth: none
}

params:path {
  eserviceId: {{eserviceId}}
}

headers {
  Authorization: {{DPOP-JWT-M2M-ADMIN}}
  DPoP: {{DPOP-PROOF}}
  Content-Type: application/merge-patch+json
}

body:json {
  {
    "name": "updated name test"
  }
}
