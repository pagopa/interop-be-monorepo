meta {
  name: Suspend eservice descriptor
  type: http
  seq: 6
}

post {
  url: {{host-m2m-gw-v3}}/eservices/:eserviceId/descriptors/:descriptorId/suspend
  body: none
  auth: none
}

params:path {
  eserviceId: {{eserviceId}}
  descriptorId: {{descriptorId}}
}

headers {
  Authorization: {{DPOP-JWT-M2M-ADMIN}}
  DPoP: {{DPOP-PROOF}}
}
