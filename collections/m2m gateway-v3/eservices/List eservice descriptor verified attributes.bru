meta {
  name: List eservice descriptor verified attributes
  type: http
  seq: 1
}

get {
  url: {{host-m2m-gw-v3}}/eservices/:eserviceId/descriptors/:descriptorId/verifiedAttributes?offset=0&limit=10
  body: none
  auth: none
}

params:query {
  offset: 0
  limit: 10
}

params:path {
  descriptorId: {{descriptorId}}
  eserviceId: {{eserviceId}}
}

headers {
  Authorization: {{DPOP-JWT-M2M}}
  DPoP: {{DPOP-PROOF}}
}
