meta {
  name: Delete eservice descriptor document
  type: http
}

delete {
  url: {{host-m2m-gw-v3}}/eservices/:eserviceId/descriptors/:descriptorId/documents/:documentId
  body: none
  auth: none
}

params:path {
  descriptorId: {{descriptorId}}
  eserviceId: {{eserviceId}}
  documentId: {{documentId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}
