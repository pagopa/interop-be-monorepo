meta {
  name: List eservice template version declared attributes
  type: http
  seq: 1
}

get {
  url: {{host-m2m-gw-v3}}/eserviceTemplates/:templateId/versions/:versionId/declaredAttributes?offset=0&limit=10
  body: none
  auth: none
}

params:query {
  offset: 0
  limit: 10
}

params:path {
  versionId: {{versionId}}
  templateId: {{templateId}}
}

headers {
  Authorization: {{JWT-M2M}}
}
