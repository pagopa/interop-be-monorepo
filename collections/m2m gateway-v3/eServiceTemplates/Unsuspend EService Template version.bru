meta {
  name: Unsuspend EService Template version
  type: http
  seq: 18
}

post {
  url: {{host-m2m-gw-v3}}/eserviceTemplates/:templateId/versions/:versionId/unsuspend
  body: json
  auth: none
}

params:path {
  versionId: {{eserviceTemplateVersionId}}
  templateId: {{eserviceTemplateId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}
