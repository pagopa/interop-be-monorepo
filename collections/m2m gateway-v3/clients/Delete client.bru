meta {
  name: Delete client
  type: http
}

delete {
  url: {{host-m2m-gw-v3}}/clients/:clientId
  body: none
  auth: none
}

params:path {
  clientId: {{clientId}}
}

headers {
  Authorization: {{DPOP-JWT-M2M-ADMIN}}
  DPoP: {{DPOP-PROOF}}
}

docs {
  Delete a Consumer Client by its ID.
}
