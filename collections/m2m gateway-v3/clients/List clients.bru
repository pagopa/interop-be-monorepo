meta {
  name: List clients
  type: http
  seq: 2
}

get {
  url: {{host-m2m-gw-v3}}/clients?offset=0&limit=10
  body: none
  auth: none
}

params:query {
  offset: 0
  limit: 10
  ~name: test
  ~consumerId: {{tenantId2}}
  ~userIds: [{{userId1}}, {{userId2}}]
  ~purposeId: "{{purposeId}}"
}

headers {
  Authorization: {{DPOP-JWT-M2M}}
  DPoP: {{DPOP-PROOF}}
}
