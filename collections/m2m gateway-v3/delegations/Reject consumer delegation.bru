meta {
  name: Reject consumer delegation
  type: http
  seq: 2
}

post {
  url: {{host-m2m-gw-v3}}/consumerDelegations/:delegationId/reject
  body: json
  auth: none
}

params:path {
  delegationId: {{delegationId}}
}

headers {
  Authorization: {{DPOP-JWT-M2M-ADMIN}}
  DPoP: {{DPOP-PROOF}}
}

body:json {
  {
    "rejectionReason": "Test reason"
  }
}
