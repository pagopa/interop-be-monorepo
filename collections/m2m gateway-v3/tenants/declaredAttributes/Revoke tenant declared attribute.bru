meta {
  name: Revoke tenant declared attribute
  type: http
  seq: 3
}

delete {
  url: {{host-m2m-gw-v3}}/tenants/:tenantId/declaredAttributes/:attributeId
  body: none
  auth: none
}

headers {
  Authorization: {{DPOP-JWT-M2M-ADMIN}}
  DPoP: {{DPOP-PROOF}}
}

params:path {
  tenantId: {{tenantId}}
  attributeId: {{attributeId}}
}
