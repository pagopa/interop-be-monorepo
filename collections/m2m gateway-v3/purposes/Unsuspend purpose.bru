meta {
  name: Unsuspend purpose
  type: http
  seq: 11
}

post {
  url: {{host-m2m-gw-v3}}/purposes/:purposeId/unsuspend
  body: json
  auth: none
}

params:path {
  purposeId: {{purposeId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}

body:json {
  {
    "delegationId": "{{delegationId}}"
  }
}
