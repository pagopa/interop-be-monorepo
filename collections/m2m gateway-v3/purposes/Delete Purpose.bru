meta {
  name: Delete purpose
  type: http
  seq: 12
}

delete {
  url: {{host-m2m-gw-v3}}/purposes/:purposeId
  body: none
  auth: none
}

params:path {
  purposeId: {{purposeId}}
}

headers {
  Authorization: {{JWT-M2M-ADMIN}}
}
