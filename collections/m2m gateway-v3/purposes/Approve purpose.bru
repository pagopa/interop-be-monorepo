meta {
  name: Approve purpose
  type: http
  seq: 10
}

post {
  url: {{host-m2m-gw-v3}}/purposes/:purposeId/approve
  body: json
  auth: none
}

params:path {
  purposeId: {{purposeId}}
}

headers {
  Authorization: {{DPOP-JWT-M2M-ADMIN}}
  DPoP: {{DPOP-PROOF}}
}

body:json {
  {
    "delegationId": "{{delegationId}}"
  }
}
