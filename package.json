{
  "private": true,
  "name": "root",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start:catalog": "turbo start --filter pagopa-interop-catalog-process",
    "start:catalog-consumer": "turbo start --filter pagopa-interop-catalog-event-consumer",
    "test": "turbo test",
    "build": "turbo build",
    "lint": "turbo lint",
    "lint:autofix": "turbo lint:autofix",
    "format:check": "turbo format:check",
    "format:write": "turbo format:write",
    "generate-model": "turbo generate-model",
    "infra:stop": "turbo infra:stop",
    "infra:destroy": "turbo infra:destroy"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^18.12.3",
    "turbo": "^1.10.7"
  },
  "packageManager": "pnpm@8.6.3"
}
