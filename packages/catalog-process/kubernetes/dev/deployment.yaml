apiVersion: apps/v1
kind: Deployment
metadata:
  name: interop-be-catalog-process-refactor
  namespace: dev-refactor
  labels:
    app: interop-be-catalog-process-refactor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: interop-be-catalog-process-refactor
  template:
    metadata:
      labels:
        app: interop-be-catalog-process-refactor
    spec:
      serviceAccountName: interop-be-catalog-process-refactor
      containers:
      - name: interop-be-catalog-process-refactor
        image: ghcr.io/pagopa/catalog-process@$IMAGE_DIGEST
        imagePullPolicy: Always
        ports:
          - name: http
            containerPort: 3000
            protocol: TCP
        resources:
          requests:
            cpu: 1.0
            memory: 2Gi
          limits:
            cpu: 1.0
            memory: 2Gi
        livenessProbe:
          httpGet:
            path: /status
            port: http
          initialDelaySeconds: 15
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /status
            port: http
          initialDelaySeconds: 15
          periodSeconds: 5
        env:
          - name: FOO
            value: BAR
