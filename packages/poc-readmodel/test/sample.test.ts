/* eslint-disable @typescript-eslint/explicit-function-return-type */
import { describe, it, expect } from "vitest";

describe("", () => {
  describe("", () => {
    it(() => {
      expect(1).toBe(1);
    });
  });
});
