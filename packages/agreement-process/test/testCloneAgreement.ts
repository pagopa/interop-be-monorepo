import { describe, expect } from "vitest";

export const testCloneAgreement = (): ReturnType<typeof describe> =>
  // TODO rebase on a PR where the file cloning is fixed
  describe("clone agreement", () => {
    expect(true).toBe(true);
  });
