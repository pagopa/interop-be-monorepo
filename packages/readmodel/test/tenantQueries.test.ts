/* eslint-disable @typescript-eslint/no-non-null-assertion */
/* eslint-disable @typescript-eslint/explicit-function-return-type */

import { describe, it, expect } from "vitest";

describe("Tenant Queris", () => {
  describe("Upsert", () => {
    it("should do something", () => {
      expect(1).toEqual(1);
    });
  });
});
